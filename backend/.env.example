# Database Configuration
DATABASE_URL=sqlite:///./taskmap_v2.db

# Google API Key for AI features
GOOGLE_API_KEY=your_google_api_key_here

# Microsoft Graph API Credentials for Teams Integration
# To get these values:
# 1. Go to https://portal.azure.com
# 2. Navigate to Azure Active Directory → App registrations
# 3. Create a new app or select existing one
# 4. Copy the Application (client) ID → MS_GRAPH_CLIENT_ID
# 5. Copy the Directory (tenant) ID → MS_GRAPH_TENANT_ID
# 6. Go to Certificates & secrets → New client secret
# 7. Copy the secret VALUE (not the ID) → MS_GRAPH_CLIENT_SECRET
#
# Required API Permissions (Microsoft Graph):
# - Chat.Read (Delegated)
# - ChannelMessage.Read.All (Application)
# - User.Read (Delegated)

MS_GRAPH_CLIENT_ID=your_azure_app_client_id_here
MS_GRAPH_CLIENT_SECRET=your_azure_app_client_secret_here
MS_GRAPH_TENANT_ID=your_azure_tenant_id_here
MS_GRAPH_SECRET_ID=your_azure_secret_id_here
MS_GRAPH_REDIRECT_URI=http://localhost:5173/auth/callback
